<% if post_image.liked_by?(current_user) %>
  <p>
    <%= link_to  post_image_likes_path(post_image.id), method: :delete  do %>
      🤍<%= post_image.likes.count %>
    <% end %>
  </p>
<% else %>
  <p>
    <%= link_to post_image_likes_path(post_image.id), method: :post do %>
      🤍<%= post_image.likes.count %>
    <% end %>
  </p>
<% end %>
